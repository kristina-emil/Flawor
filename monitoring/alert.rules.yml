groups:
  - name: instance-health
    rules:
      - alert: FlavorAppDown
        expr: up{job="flavor-app"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Flavor App is down"
          description: "No metrics received from flavor-app (instance {{ $labels.instance }})"
